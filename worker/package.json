{
  "name": "quizflash-ai-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "prompt:publish": "node --experimental-strip-types scripts/publishPromptBundle.mjs",
    "pricing:publish": "node scripts/publishPricingConfig.mjs",
    "plan-limit:set": "node scripts/setPlanLimit.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.62",
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.103.0"
  }
}
