{
  "name": "quizflash-ai-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "prompt:publish": "node scripts/publishPromptBundle.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.62",
    "@cloudflare/workers-types": "^4.20260621.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.103.0"
  }
}
