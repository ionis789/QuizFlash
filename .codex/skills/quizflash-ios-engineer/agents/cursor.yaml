interface:
  display_name: "QuizFlash iOS Engineer"
  short_description: "QuizFlash SwiftUI + SwiftData coding guide (iOS 17+)"
  default_prompt: "Use $quizflash-ios-engineer rules for all non-trivial QuizFlash changes. Prefer existing primitives (NavigationManager, UIConstants, CardFetchActor, fullScreenSheet). Avoid performance regressions and iOS 17 SwiftData relationship predicates. Output exact file edits and the command to validate with xcodebuild."
